{
  "devDependencies": {},
  "name": "insure-app",
  "version": "0.1.0",
  "private": true,  
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "json-server --watch ./src/db/db.json --port 3001",
    "backfront": "json-server --watch db/db.json --port 3001 --static ./app"
  },
  "author": "Júlia de Souza Borges",
  "license": "ISC",
  "description": ""
}
